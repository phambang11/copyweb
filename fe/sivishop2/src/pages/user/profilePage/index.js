import React, {memo} from 'react';
import './styles.scss';
const ProfilePage = () => {
    return (
        <h1>
            Body - ProfilePage
        </h1>
    );
};

export default memo(ProfilePage);